const entryvalidate = {
    name: 'string',
    sex: 'string',
    clas: 'string',
    studentid: 'string',
    password: 'string'
}

const logindata = {
    studentid: 'string',
    password: 'string'
}

module.exports = { entryvalidate, logindata };