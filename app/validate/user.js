const entryvalitemp = {
    record: 'string',
}

const entryvalipass = {
    oldpassword: 'string',
    password: 'string',
    conpassword: 'string',
}


const entryvaliemail = {
    email: 'email',
}

module.exports = { entryvalitemp, entryvalipass, entryvaliemail };