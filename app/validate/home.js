const entryvalidate = {
    name: 'string',
    wuhan: ['0', '1'],
    sex: 'string',
    clas: 'string',
    studentid: 'string',
    city: 'string',
    password: 'string'
}

const logindata = {
    studentid: 'string',
    password: 'string'
}

module.exports = { entryvalidate, logindata };