const entryvalitemp = {
    record: 'string',
}

const entryvalipass = {
    oldpassword: 'string',
    password: 'string',
    conpassword: 'string',
}

const entryvaltravel = {
    travel: 'string',
}

module.exports = { entryvalitemp, entryvalipass, entryvaltravel };